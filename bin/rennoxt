#!/bin/bash

# Rename files preserving old file name extension

OLDNAME=$1
KEEPEXTENSION=$(echo $OLDNAME | awk -F '.' '{print $NF}')
REMOVEEXTENSION=$(echo $2 | awk -F '.' '{print $NF}')
NEWNAME=$(echo $2 | sed "s/$REMOVEEXTENSION//g")
mv "$OLDNAME" "$NEWNAME""$KEEPEXTENSION" &> /dev/null
