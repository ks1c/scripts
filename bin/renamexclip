#!/bin/bash

# Rename file with clipboard content preserving old file name extension

OLDNAME=$1
NEWNAME=$(xclip -o | awk -F '.' '{print $1}')
EXTENSION=$(echo $OLDNAME | awk -F '.' '{print $2}')
mv "$OLDNAME" "$NEWNAME"."$EXTENSION"
